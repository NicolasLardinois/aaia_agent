# Block 3a: PM-Ausbau — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** Portfolio-Domänenmodell + `PortfolioPort` + `JsonPortfolioProvider`; `PortfolioMonitorAgent` auf den Port umstellen (richtungs-bewusste P&L + netto/brutto-Exposure + Klumpen auf netto); `current_position` aus dem echten Depot.

**Architecture:** Sauber hexagonal — `Position`-Modell, Port (ABC), JSON-Adapter; Konsumenten per Injektion.

**Tech Stack:** Python, dataclasses, ABC, pytest.

## Global Constraints
- Spec: `docs/superpowers/specs/2026-06-20-pm-ausbau-3a-design.md`. Design: `docs/short.md` §12.
- `direction` ist **Pflicht** (kein Default); fehlend/ungültig → `PortfolioError` (fail-loud), Konsumenten degradieren mit Alarm/Warnung.
- Klumpen auf **netto** pro Bucket (Pairtrades netten sich aus); netto+brutto-Exposure ausweisen.
- Branch `feat/pm-ausbau`. PR-First. Runner `python -m pytest -q`. Am Ende (Task 4) Gesamtsuite grün.

---

## Task 1: `Position`-Modell + `PortfolioError` + `PortfolioPort`

**Files:** Create `core/domain/portfolio.py`, `core/ports/portfolio_port.py`; Test `tests/test_portfolio_model_port.py` (Create).

- [ ] **Step 1: Failing test** — `tests/test_portfolio_model_port.py`:
```python
import inspect
from core.domain.portfolio import Position, PortfolioError
from core.ports.portfolio_port import PortfolioPort


def test_position_requires_direction():
    p = Position(ticker="AAPL", shares=10, entry_price=150.0, direction="short")
    assert p.direction == "short" and p.entry_price == 150.0


def test_portfolio_error_is_exception():
    assert issubclass(PortfolioError, Exception)


def test_port_is_abstract():
    assert inspect.isabstract(PortfolioPort)
    assert {"get_positions", "position_state_for"} <= set(dir(PortfolioPort))
```

- [ ] **Step 2: Run → FAIL** — `python -m pytest tests/test_portfolio_model_port.py -q`.

- [ ] **Step 3: Implement** — `core/domain/portfolio.py`:
```python
from dataclasses import dataclass
from typing import Optional


@dataclass(frozen=True)
class Position:
    ticker: str
    shares: float
    entry_price: float          # aus JSON-Key "buy_price"
    direction: str              # "long" | "short" — PFLICHT
    currency: str = "USD"
    current_price: Optional[float] = None
    sector: str = "Unbekannt"
    asset_class: str = "equity"
    country: str = "Unbekannt"


class PortfolioError(Exception):
    """Ungültige/fehlende Positionsdaten (z. B. fehlende direction)."""
```
und `core/ports/portfolio_port.py`:
```python
from abc import ABC, abstractmethod

from core.domain.models import PositionState
from core.domain.portfolio import Position


class PortfolioPort(ABC):
    @abstractmethod
    def get_positions(self) -> list[Position]: ...

    @abstractmethod
    def position_state_for(self, ticker: str) -> PositionState: ...
```

- [ ] **Step 4: Run → PASS** — `python -m pytest tests/test_portfolio_model_port.py -q`.

- [ ] **Step 5: Commit** — `git add core/domain/portfolio.py core/ports/portfolio_port.py tests/test_portfolio_model_port.py && git commit -m "feat(portfolio): Position-Modell + PortfolioError + PortfolioPort"`

---

## Task 2: `JsonPortfolioProvider` (Adapter)

**Files:** Create `adapters/persistence/json_portfolio.py`; Test `tests/test_json_portfolio.py` (Create).

- [ ] **Step 1: Failing test** — `tests/test_json_portfolio.py`:
```python
import json
import pytest
from core.domain.models import PositionState
from core.domain.portfolio import PortfolioError
from adapters.persistence.json_portfolio import JsonPortfolioProvider


def _write(tmp_path, positions):
    f = tmp_path / "portfolio.json"
    f.write_text(json.dumps({"positions": positions}), encoding="utf-8")
    return str(f)


def test_valid_long_and_short(tmp_path):
    path = _write(tmp_path, [
        {"ticker": "AAPL", "shares": 10, "buy_price": 150, "direction": "long",
         "sector": "Tech", "asset_class": "equity", "country": "USA", "currency": "USD"},
        {"ticker": "NOK", "shares": 50, "buy_price": 4, "direction": "short"},
    ])
    p = JsonPortfolioProvider(path)
    pos = p.get_positions()
    assert {x.ticker for x in pos} == {"AAPL", "NOK"}
    assert p.position_state_for("AAPL") == PositionState.LONG
    assert p.position_state_for("NOK") == PositionState.SHORT
    assert p.position_state_for("MSFT") == PositionState.NONE


def test_missing_direction_raises(tmp_path):
    path = _write(tmp_path, [{"ticker": "AAPL", "shares": 10, "buy_price": 150}])
    with pytest.raises(PortfolioError):
        JsonPortfolioProvider(path).get_positions()


def test_invalid_direction_raises(tmp_path):
    path = _write(tmp_path, [{"ticker": "X", "shares": 1, "buy_price": 1, "direction": "neutral"}])
    with pytest.raises(PortfolioError):
        JsonPortfolioProvider(path).get_positions()


def test_missing_file_is_empty(tmp_path):
    p = JsonPortfolioProvider(str(tmp_path / "nope.json"))
    assert p.get_positions() == []
    assert p.position_state_for("AAPL") == PositionState.NONE
```

- [ ] **Step 2: Run → FAIL** — `python -m pytest tests/test_json_portfolio.py -q`.

- [ ] **Step 3: Implement** — `adapters/persistence/json_portfolio.py`:
```python
import json
import os

from core.domain.models import PositionState
from core.domain.portfolio import Position, PortfolioError
from core.ports.portfolio_port import PortfolioPort

_DEFAULT_PATH = os.path.join(os.path.dirname(__file__), "..", "..", "data", "portfolio.json")
_VALID_DIR = {"long", "short"}


class JsonPortfolioProvider(PortfolioPort):
    def __init__(self, path: str = _DEFAULT_PATH):
        self.path = path

    def get_positions(self) -> list[Position]:
        try:
            with open(self.path, "r", encoding="utf-8") as f:
                data = json.load(f)
        except FileNotFoundError:
            return []
        out = []
        for d in data.get("positions", []):
            direction = d.get("direction")
            ticker = d.get("ticker", "?")
            if direction not in _VALID_DIR:
                raise PortfolioError(
                    f"Position {ticker}: direction fehlt/ungültig ({direction!r}) — "
                    f"muss 'long' oder 'short' sein.")
            out.append(Position(
                ticker=ticker, shares=d["shares"], entry_price=d["buy_price"],
                direction=direction, currency=d.get("currency", "USD"),
                current_price=d.get("current_price"),
                sector=d.get("sector", "Unbekannt"),
                asset_class=d.get("asset_class", "equity"),
                country=d.get("country", "Unbekannt")))
        return out

    def position_state_for(self, ticker: str) -> PositionState:
        for p in self.get_positions():
            if p.ticker == ticker:
                return PositionState.LONG if p.direction == "long" else PositionState.SHORT
        return PositionState.NONE
```

- [ ] **Step 4: Run → PASS** — `python -m pytest tests/test_json_portfolio.py -q`.

- [ ] **Step 5: Commit** — `git add adapters/persistence/json_portfolio.py tests/test_json_portfolio.py && git commit -m "feat(portfolio): JsonPortfolioProvider (liest + validiert direction)"`

---

## Task 3: `PortfolioMonitorAgent` auf Port + richtungs-bewusst

**Files:** Modify `agents/portfolio/portfolio_monitor_agent.py`, `background_runner.py`, `tests/test_portfolio_monitor.py`.

**ZUERST** `agents/portfolio/portfolio_monitor_agent.py` + `tests/test_portfolio_monitor.py` komplett lesen.

- [ ] **Step 1: Tests anpassen (failing)** — `tests/test_portfolio_monitor.py` auf `Position`-Objekte + injizierten Port umstellen und neue Fälle ergänzen. Muster:
```python
from unittest.mock import MagicMock
from core.domain.portfolio import Position
from agents.portfolio.portfolio_monitor_agent import PortfolioMonitorAgent


def _port(positions):
    p = MagicMock()
    p.get_positions.return_value = positions
    return p


def _agent(positions):
    mem = MagicMock()
    return PortfolioMonitorAgent(mem, portfolio_port=_port(positions),
                                 fx_rate=lambda a, b: 1.0)


def _pos(ticker, direction, entry, cur, sector="Tech", shares=10):
    return Position(ticker=ticker, shares=shares, entry_price=entry, direction=direction,
                    current_price=cur, sector=sector, asset_class="equity", country="USA")


def test_short_pnl_inverted_triggers_loss_on_rise():
    # Short bei 100, Kurs steigt auf 130 → -30% (Verlust für Short)
    snap = _agent([_pos("X", "short", 100, 130)])._evaluate_positions(
        _agent([_pos("X", "short", 100, 130)]).portfolio_port.get_positions())
    assert any("X" in a for a in snap["alerts"])


def test_pairtrade_no_cluster_alert():
    # long A + short B, gleicher Sektor, gleicher Wert → Netto-Sektor ~0
    positions = [_pos("A", "long", 100, 100, sector="Tech"),
                 _pos("B", "short", 100, 100, sector="Tech")]
    snap = _agent(positions)._evaluate_positions(positions)
    assert not any("Klumpen" in a for a in snap["alerts"])
    assert snap["net_exposure"] == 0.0
    assert snap["gross_exposure"] > 0.0
```
> Bestehende Tests (long-only Klumpen, Verlust, HHI, vol) auf `Position`-Objekte + `portfolio_port`-Injektion umstellen; die fachlichen Erwartungen bleiben (reines Long-Klumpen schlägt weiter an).

- [ ] **Step 2: Run → FAIL** — `python -m pytest tests/test_portfolio_monitor.py -q`.

- [ ] **Step 3: Implement** — `agents/portfolio/portfolio_monitor_agent.py`:
  - Importe: `from core.ports.portfolio_port import PortfolioPort`, `from core.domain.portfolio import PortfolioError`.
  - `__init__(self, memory, portfolio_port: PortfolioPort, market_provider=None, fx_rate=_default_fx_rate, returns_provider=None)` — `self.portfolio_port = portfolio_port`. (`PORTFOLIO_FILE`/direkte Datei-Lesung entfernen.)
  - `_check_cluster_risks` auf **netto** umstellen:
    ```python
    def _check_cluster_risks(positions, values, gross):
        if gross == 0:
            return []
        risks = []
        thresholds = {"sector": SECTOR_THRESHOLD, "asset_class": ASSET_CLASS_THRESHOLD, "country": COUNTRY_THRESHOLD}
        for dim, threshold in thresholds.items():
            buckets = {}
            for p, val in zip(positions, values):
                signed = val if p.direction == "long" else -val
                name = getattr(p, dim, "Unbekannt")
                buckets[name] = buckets.get(name, 0.0) + signed
            for name, net in buckets.items():
                pct = abs(net) / gross
                if pct > threshold:
                    risks.append({"type": dim, "name": name, "pct": round(pct, 3),
                                  "threshold": threshold,
                                  "message": f"Klumpenrisiko {dim.replace('_','-').title()}: "
                                             f"{name} = {pct:.0%} netto (Grenze: {threshold:.0%})"})
        return risks
    ```
  - `_evaluate_positions(self, positions: list[Position])`: auf `Position`-Attribute umstellen:
    ```python
    if not positions:
        return {... "long_value":0.0,"short_value":0.0,"net_exposure":0.0,"gross_exposure":0.0, ...}
    values, longs, shorts = [], 0.0, 0.0
    cur_by_ticker = {}
    for p in positions:
        cur = p.current_price if p.current_price is not None else (_fetch_current_price(p.ticker) or p.entry_price)
        cur_by_ticker[p.ticker] = cur
        val = p.shares * cur * self.fx_rate(p.currency, BASE_CURRENCY)
        values.append(val)
        longs += val if p.direction == "long" else 0.0
        shorts += val if p.direction == "short" else 0.0
    net, gross = longs - shorts, longs + shorts
    cluster_risks = _check_cluster_risks(positions, values, gross)
    alerts = [r["message"] for r in cluster_risks]
    for p in positions:
        if p.entry_price > 0:
            cur = cur_by_ticker[p.ticker]
            pnl = ((cur - p.entry_price)/p.entry_price) if p.direction == "long" else ((p.entry_price - cur)/p.entry_price)
            if pnl < -LOSS_THRESHOLD:
                alerts.append(f"Offener Verlust {p.ticker} ({p.direction}): {pnl:.0%} "
                              f"(Einstand {p.entry_price:.2f}, Heute {cur:.2f})")
    # Memory-Alignment-Warnungen wie bisher (p.ticker statt p["ticker"]).
    # HHI über Brutto-Gewichte: weights = [v/gross for v in values] if gross>0 else []
    # vol/MaxDD: Gewicht je Position SIGNED (long +, short -) * (v/gross), sonst wie bisher.
    ```
    Restliche Logik (HHI, vol/MaxDD, health) übernehmen, nur `p["..."]` → `p.<attr>` und Gewichte wie oben. Snapshot um `long_value`/`short_value`/`net_exposure`/`gross_exposure` (gerundet) erweitern.
  - `run()`: statt Datei lesen → `try: positions = self.portfolio_port.get_positions() except PortfolioError as e: print(f"[PortfolioMonitor] Portfolio-Daten ungültig: {e}"); return`. Leer → wie bisher („Keine Positionen").
  - `background_runner.py`: `PortfolioMonitorAgent(memory, portfolio_port=JsonPortfolioProvider())` (Import ergänzen).

- [ ] **Step 4: Run → PASS** — `python -m pytest tests/test_portfolio_monitor.py -q`.

- [ ] **Step 5: Commit** — `git add agents/portfolio/portfolio_monitor_agent.py background_runner.py tests/test_portfolio_monitor.py && git commit -m "feat(portfolio): Monitor auf Port + richtungs-bewusste P&L/Exposure + Klumpen netto"`

---

## Task 4: `current_position` aus dem Depot + Gesamt-Regression

**Files:** Modify `app/main.py`; ggf. Tests.

- [ ] **Step 1: Implement** — `app/main.py`:
  - Import: `from adapters.persistence.json_portfolio import JsonPortfolioProvider`; `from core.domain.portfolio import PortfolioError`.
  - In `run_judgment(ticker, market="USA")` (Parameter `current_position` **entfernen**): am Anfang
    ```python
    try:
        current_position = JsonPortfolioProvider().position_state_for(ticker)
    except PortfolioError as e:
        print(f"⚠ Portfolio-Daten ungültig ({e}) — current_position=NONE.")
        current_position = PositionState.NONE
    ```
  - Das **`--position`-Argument-Parsing entfernen** (der Block mit `args.index("--position")`); `run_judgment(args[1], market=market)` aufrufen. Usage-Doc von `--position` bereinigen.

- [ ] **Step 2: Restliche `--position`/`current_position`-Stellen** — `grep -rn "\-\-position\|current_position=" app/ tests/`; verbliebene Aufrufe anpassen.

- [ ] **Step 3: Gesamt-Regression** — `python -m pytest -q` → **0 failed** (~3 Min). Bei Fehlern: superpowers:systematic-debugging.

- [ ] **Step 4: Commit** — `git add app/ tests/ && git commit -m "feat(portfolio): current_position aus Depot (Lookup) statt CLI-Flag + Regression gruen (3a)"`

---

## Abdeckung (Spec → Task)
| Spec-Element | Task |
|---|---|
| `Position` + `PortfolioError` + `PortfolioPort` | 1 |
| `JsonPortfolioProvider` (lesen + Validierung) | 2 |
| Monitor: Port + Short-P&L + netto/brutto + Klumpen-netto | 3 |
| `current_position` aus Depot, CLI-Flag entfernt | 4 |
| Fail-loud + Degradierung (Alarm/Warnung) | 2, 3, 4 |

## Self-Review (durchgeführt)
- **Spec-Abdeckung:** alle Komponenten je Task. ✅
- **Platzhalter:** Modell/Port/Adapter vollständig; Monitor-Refactor mit konkreten Funktionen + Lese-Anweisung für die unveränderten Teile. ✅
- **Typ-Konsistenz:** `position_state_for -> PositionState`; `get_positions -> list[Position]`; Monitor nutzt `Position`-Attribute; `portfolio_port` in __init__ (Task 3) + background_runner/Tests. ✅
